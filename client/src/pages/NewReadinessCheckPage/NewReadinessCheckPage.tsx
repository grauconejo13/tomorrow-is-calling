import { useState, type FormEvent } from "react"; import type { ReadinessCheckForm } from "../../types/shipment"; import "./NewReadinessCheckPage.css";
const fields:[keyof ReadinessCheckForm,string,string][]=[["reference","Shipment reference","text"],["cargo","Cargo or operation description","text"],["origin","Origin","text"],["destination","Destination","text"],["carrier","Carrier","text"],["cutoffDate","Cutoff date","date"],["cutoffTime","Cutoff time","time"],["contact","Responsible contact","text"],["phone","Phone number","tel"]];
export function NewReadinessCheckPage({initial,onReview,onCancel}:{initial:ReadinessCheckForm;onReview:(data:ReadinessCheckForm)=>void;onCancel:()=>void}){const [data,setData]=useState(initial);const [errors,setErrors]=useState<string[]>([]);const update=(key:keyof ReadinessCheckForm,value:string|boolean)=>setData(current=>({...current,[key]:value}));function submit(event:FormEvent){event.preventDefault();const missing=fields.filter(([key])=>!data[key]).map(([,label])=>label);if(!data.consent)missing.push("Consent");setErrors(missing);if(!missing.length)onReview(data)}return <main className="page form-page"><p className="eyebrow">New readiness check</p><h1>Prepare an operation review</h1><p className="page-intro">All fields are prefilled with fictional prototype data. No call will be placed.</p>{errors.length>0&&<div className="form-error" role="alert">Complete: {errors.join(", ")}.</div>}<form onSubmit={submit} noValidate><div className="form-grid">{fields.map(([key,label,type])=><label key={key}>{label}<input type={type} value={String(data[key])} onChange={e=>update(key,e.target.value)} required aria-invalid={errors.some(error=>error===label)}/>{key==="phone"&&<small>Prototype mock data only. No call will be placed.</small>}</label>)}</div><label>Known concerns<textarea value={data.concerns} onChange={e=>update("concerns",e.target.value)} required rows={4}/></label><fieldset><legend>Call timing</legend><label><input type="radio" checked={data.callTiming==="now"} onChange={()=>update("callTiming","now")}/> Call now</label><label><input type="radio" checked={data.callTiming==="schedule"} onChange={()=>update("callTiming","schedule")}/> Schedule</label></fieldset><label className="consent"><input type="checkbox" checked={data.consent} onChange={e=>update("consent",e.target.checked)} required/> I confirm I have permission to contact the responsible person for this readiness check.</label><div className="form-actions"><button className="button button--primary" type="submit">Review readiness call</button><button className="button button--quiet" type="button" onClick={onCancel}>Cancel</button></div></form></main>}
