import { demoReport } from "../../data/demoShipment";
import { StatusBadge } from "../../components/StatusBadge/StatusBadge";
import "./ReadinessReportPage.css";
export function ReadinessReportPage({ onNew, onOverview }: { onNew: () => void; onOverview: () => void }) { const { outcome } = demoReport; return <main className="page report"><p className="eyebrow">Customer follow-up outcome</p><div className="report__title"><div><h1>Call outcome <span>AT-4821</span></h1><p>Maya Chen · 2022 Toyota Camry</p></div><StatusBadge tone="attention">Follow-up required</StatusBadge></div><section className="report-metrics"><div><b>Yes</b><span>Customer reached</span></div><div><b>Before 4 PM</b><span>Expected completion</span></div><div><b>Pending</b><span>Alternate authorization</span></div><div><b>Yes</b><span>Human follow-up</span></div></section><section><p className="section-label">Confirmed information</p><div className="risk-list">{outcome.confirmed.map((item) => <article className="risk-item" key={item.label}><div><StatusBadge tone="ready">Confirmed</StatusBadge><h3>{item.label}</h3></div><p>{item.value}</p></article>)}</div></section><section><p className="section-label">Unresolved items</p><div className="risk-list">{outcome.unresolved.map((item) => <article className="risk-item" key={item.label}><div><StatusBadge tone="critical">Pending</StatusBadge><h3>{item.label}</h3></div><p>{item.value}</p></article>)}</div></section><section className="projection"><p className="section-label">Next action</p><div><p>{outcome.nextAction}</p><p><b>Human escalation:</b> {outcome.humanEscalation ? "Required — alternate-contact authorization needs review." : "Not required."}</p><p><b>Notes:</b> {outcome.notes}</p></div></section><div className="form-actions"><button className="button button--primary" onClick={onNew}>Create another request</button><button className="button button--quiet" onClick={onOverview}>Return to transport queue</button></div></main>; }
